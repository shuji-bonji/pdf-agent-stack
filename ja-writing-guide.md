# Grok Build 向け指示 — PDF Agent Stack の日本語

このファイルは、PDF Agent Stack サイト（`site/docs/ja/`）の日本語を書く・直すときに、Grok Build が従う指示である。
対象リポジトリ: https://github.com/shuji-bonji/pdf-agent-stack
公開サイト: https://shuji-bonji.github.io/pdf-agent-stack/ja/

英語ページ（`site/docs/` の `ja/` 以外）の事実・識別子・数値は変えない。変えるのは日本語の形だけである。一次執筆は日本語、英語は翻訳先、という運用は `site/DESIGN.md` の i18n 方針に従う。

---

## 目的

読者が、その段落だけを開いても、次を復元できるように書く。

- 誰が（サーバー名、ツール名、ホスト名、モデルの種類）
- 何を入力し、何が返るか（フィールド名、コマンド、エラー文）
- 何が起きないか（判定しない、準拠を証明しない、ファイルを開かない）

英語 1 語を漢字 1 語に載せない。比喩と擬人化を使わない。一語に 2 つの対象を持たせない。

---

## 書いてよいこと / 書いてはいけないこと

書いてよい。

- `site/docs/ja/` 配下の Markdown の日本語
- 既存の frontmatter（`description` など）の日本語。識別子と URL は維持する
- VitePress の内部リンク形式（例: `[pdf-trust](/ja/skills/pdf-trust)`）
- 用語集に既にある定義語を、その定義のまま使うこと

書いてはいけない。

- ツール名、引数名、環境変数、4 値判定の値、T1 / T2 / T3 の対象規格を変えること
- 「準拠している」「規格を満たす」と言い切ること（T2 / T3。T1 でも条文引用なしでは言わない）
- サイトに無い倍率、性能、対応モデル名を足すこと
- `ja/` 以外の英語本文を、依頼されていないのに書き換えること
- 仕様 PDF をリポジトリやサイトに置くこと

言い切り強度はページ本文にも適用する（`site/DESIGN.md` §8）。

- T1（ISO 32000 / PDF/UA）: 条文を引用して言い切る
- T2（PDF/A）: 「veraPDF が COMPLIANT と判定」とだけ書く
- T3（PAdES）: 構造の観測として述べる。「準拠」とは書かない

---

## 書く前の 3 つの確認

1 つでも当たったら書き直す。

### 1. 逆翻訳

その語を英語に戻すと専門語 1 語になるか。なるなら英語の直訳であり、日本語としては対象を指していない。

- ×「前提が逆転する」← premise inverts
- ○「Lv1 は大型モデルが instructions を守ることを前提にしている。ローカルの小型モデルではその前提は成り立たない」
- ×「器の読み替え」← vessel
- ○「Skill / サブエージェント定義は Claude Code が読む形式である。ローカルでは system prompt と、呼び出し順を固定したコードに書く」

### 2. 錨

直前の段落が無い状態で読んでも、同じ対象を指すか。

- ×「そのまま効くもの」
- ○「判定を `evaluate_policy` に置くこと、渡すツールを絞ること、T1 / T2 / T3 を守ること」

### 3. 名前

実行すれば目に見える名前があるなら、その名前で書く。

- ×「ジャッジ」
- ○「`evaluate_policy` が返す 4 値判定」
- ×「緑のテスト」
- ○「テストが通っている。ただしフィクスチャ不在やガード節で、判定が実行されていないことがある」

一語が 2 つを指していたら分ける。

- ×「ラベル ≠ 規格どおり」
- ○「`ensure_pdfa` が書くのは XMP のラベルである。規格どおりかどうかは `validate_conformance` で測る」

---

## サイト全体で使わない語 / 使う語

左は本文に残さない。右で書く。用語集（`site/docs/ja/reference/glossary.md`）に定義がある語は、定義と矛盾させない。

| 使わない | 使う | 理由 |
|---|---|---|
| 自然に守る | instructions を読んで境界の内側に留まりやすい（大型モデルの場合） | 「自然」は何が起きるかを書いていない |
| 効く / 効きます | 宣言がホストに渡る / モデルが読む / 呼び出し順が固定される、のどれかを書く | 3 対象が 1 語に畳まれている |
| 前提が逆転する | 前提が成り立たない。成り立たない条件を書く | invert の直訳 |
| 器 / 器の読み替え | Claude Code が読む形式 / system prompt とパイプラインのコード | 「器」は比喩 |
| 組み上げる | 組む / 登録する / 定義する | 何のファイルを置くかを書く |
| 自己申告する | MCP instructions で責務境界を宣言する | 申告は人の行為に読める |
| ジャッジはコード | 判定は `evaluate_policy` が返す。LLM は説明文だけを書く | 用語集の「ナラティブ」は説明文と書いてよい |
| 能力の制限が信頼性になる | 監査エージェントに `pdf-writer` を渡さない。編集できなければ監査中にファイルを変えられない | 抽象名詞を動作に戻す |
| 緑のテストは空振りしうる | テストは通るが、判定が実行されていないことがある | 「緑」「空振り」は比喩 |
| 署名を壊す | 署名を無効にする | 「壊す」はファイル破損にも読める |
| 設計されています | 主語と動作を書く（登録する、宣言する、固定する） | 主体が文書の外に逃げる |
| モデル非依存 | クラウドの大型モデルでも、ローカルの小型モデルでも同じ | 何が同じかを列挙する |
| Lv3 的な | Lv3 のサブエージェント定義がやっていることと同じ。固定する場所が違う | 「的」は範囲が不定 |
| フォールバック（本文で説明なし） | 「A が使えないときは B を使う」。対象を書く | 用語集にある語でも、初出では動作を書く |
| 提供する（連続） | 返すもの / できること を列挙する | 主語を増やさずに繰り返さない |

残してよい語（用語集または本サイトの定義済み）。

- 宣言 / 準拠 / 検証 / 観測 / 判定
- 言い切り強度、T1 / T2 / T3
- 4 値判定（`trust_and_use` 等は原語のまま）
- MCP instructions、Skill、サブエージェント、frontmatter
- 決定論的（初出では「同じ入力なら同じ結果」を添える）
- コーパス、veraPDF、PAdES、LTV
- `evaluate_policy`、`ensure_pdfa`、`validate_conformance`

「ナラティブ」は用語集にある。読者向け本文では「説明文」を優先する。用語集への参照では「ナラティブ」を使ってよい。

---

## 文の形

- です・ますに揃える。コード例の中のプロンプトは「である」調のままでよい（サブエージェント定義の原文）。
- 動詞の直後の全角コロン「：」は使わない。列挙の前は「次のとおりです。」または「次です。」
- 主語を省略して「エージェント」と「サーバー」と「モデル」を混ぜない。
- Claude Code とローカル LLM を同一主語にしない。ランタイムが違う。
- 範囲は「3.9〜3.12」のように波ダッシュに揃える。
- ツール名、引数、環境変数、エラー文はバッククォートで原文のまま書く。

ページ種別ごとの型（`site/DESIGN.md` §5）。型を崩して新しい見出し体系を作らない。

| 種別 | 順 |
|---|---|
| MCP ページ | 責務 → インストール → 環境変数 → ツール一覧 → ツール別 → エラー → 限界 |
| ユースケース | シナリオ → 登場 MCP/Skill → シーケンス図 → プロンプト例 → 結果の読み方 |
| ガイド | 既存の見出し順を維持する。レベル番号（Lv1 / Lv2 / Lv3）は変えない |

各ツール説明には「できること」「できないこと」「次に呼ぶツール」を残す。

---

## 「ローカル LLM」を書くときの固定内容

この区別は `guide/agents.md` 以外のページでも崩さない。

同じ（モデルの種類を問わない）。

- 判定は `evaluate_policy` の戻り値を転記する
- 監査には `pdf-writer` を渡さない
- 言い切りは T1 / T2 / T3

違う（Lv1 の前提）。

- Claude Code + 大型モデル: 4 サーバー登録のあと、MCP instructions を読んで境界の内側に留まりやすい
- ローカルの小型モデル: ツール呼び出しの JSON が崩れる。instructions だけでは reader / writer の越境を止められない

載せる先。

- Claude Code: Skill Markdown、サブエージェント定義の frontmatter（`tools`）
- ローカル: system prompt、呼び出し順を固定したコード（例: `pdf-agent-pipeline`）

「Lv1 から始める」は Claude Code 向けの話である。ローカル向けには書かない。

---

## 作業手順

1. 直すページの `site/docs/ja/...` と、対になる英語 `site/docs/...` を両方読む。
2. 識別子・数値・コマンドを控える。日本語だけで事実を足さない。
3. 用語集と本指示の「使わない語」で検索する。左列が残っていたら右列に置換する。
4. 3 つの確認を段落ごとに行う。
5. 変更は依頼されたページに限る。サイト全体を勝手に書き換えない。
6. コミットする場合の対象パスは `site/docs/ja/` と、この指示ファイル自身に限る（依頼があれば）。

英語から新規に訳すときの変換例。

```
EN: the principles are model-independent, but Lv1's premise inverts
NG: 原則はモデル非依存ですが、Lv1 だけは前提が逆転します
OK: 判定の置き方とツールの制限と T1/T2/T3 は、モデルの種類を問いません。
    Lv1 だけは前提が違います。大型モデルが MCP instructions を守る、という前提は、
    ローカルの小型モデルでは成り立ちません。
```

```
EN: Skills / subagent definitions are Claude Code vessels
NG: Skill / サブエージェント定義は Claude Code の器です
OK: Skill ファイルとサブエージェント定義は、Claude Code が読む形式です。
    ローカル LLM のランタイムは、同じファイルを Skill としては読みません。
```

```
EN: an agent naturally stays inside these boundaries
NG: エージェントが自然に守るよう設計されています
OK: 大型モデルは、MCP instructions を読んで境界の内側に留まりやすいです。
    登録しただけで呼び出し順まで固定されるわけではありません。
```

---

## 参照

- 用語の正: `site/docs/ja/reference/glossary.md`
- サイト構造の正: `site/DESIGN.md`
- 4 層と責務の正: `site/docs/ja/guide/architecture.md`
- ローカルで呼び出し順をコード固定する実装例: https://github.com/shuji-bonji/pdf-agent-pipeline
- 同一作者の別リポジトリでの日本語点検: https://github.com/shuji-bonji/xcomet-mcp-server/blob/main/ja-writing-guide.md
